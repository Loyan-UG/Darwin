# Darwin Backlog

Reviewed: 2026-05-20

This file is the active roadmap for go-live planning. Historical implementation notes live in `docs/implementation-ledger.md`; do not expand this file with detailed implementation ledger entries. Code-backed status is tracked in `docs/go-live-status.md` and the module audit matrix in `docs/module-audit.md`. The full documentation map is `docs/README.md`.

## Current Go-Live Blockers

These are blocked on external credentials, deployment choices, provider account setup, or legal/compliance tooling decisions. Internal code-backed work that can be completed without those inputs is tracked in `docs/go-live-status.md`, `docs/module-audit.md`, and `docs/compliance-decisions.md`.

- `Stripe`: repeat the provider smoke with live keys before production traffic. The WebApi source rejects local/mock Stripe handoff fallback and requires provider-hosted Checkout Session creation for storefront payments and business subscription checkout. WebAdmin refund creation calls the Stripe refund API for Stripe payments with provider references and stores provider refund status/failure details for operator review. On 2026-05-24, the `https://api.loyan.de` test-mode smoke created a disposable storefront order, returned a Stripe-hosted Checkout Session, confirmed the return route left payment `Pending`, completed hosted test-card payment, received signed Stripe webhook events, and processed them through `ProviderCallbackWorker` so the Darwin payment moved to captured/provider-finalized state. The same run paid a business subscription Checkout Session, processed signed subscription events, updated the matching `BusinessSubscription` with provider references, created a WebAdmin provider-backed Stripe refund, received signed `charge.refunded`, reconciled the refund, subscribed the Stripe endpoint to `charge.dispute.*` events, and verified a real test-mode `charge.dispute.created` webhook marks the matched payment for dispute follow-up. Remaining Stripe blocker: repeat an approved smoke with live keys and real operational monitoring before production traffic.
- `DHL`: run live smoke with the production account. Confirm account-specific base URL, authentication, product codes, outbound shipment payload, return-label payload, label PDF retrieval/storage, tracking reference persistence, callback processing, and WebAdmin recovery for failed/stuck provider operations. A guarded local prerequisite/validation harness exists at `scripts/smoke-dhl-live.ps1`; real smoke remains blocked until approved account settings are configured.
- `Brevo`: complete production DNS/DKIM/DMARC/webhook/worker verification: sender domain verification, DKIM/DMARC, sandbox off, Basic Auth webhook configured, transactional webhook events subscribed, `ProviderCallbackWorker` and `EmailDispatchOperationWorker` enabled. A guarded sandbox/controlled-inbox harness exists at `scripts/smoke-brevo-readiness.ps1`; its `-RequireDeliveryPipeline` preflight now blocks controlled-inbox smoke until the public webhook endpoint, subscription, and worker confirmations are present. Real production readiness still requires account DNS, delivery, and webhook verification.
- `VAT/VIES`: live-smoke VIES. Phase 1 policy remains soft `Unknown` plus manual review when VIES is disabled, unavailable, rate-limited, malformed, or timed out. Do not record false Valid/Invalid decisions on provider failure. Internal provider policy coverage and a disabled-by-default retry worker now exist; a guarded direct VIES harness exists at `scripts/smoke-vies-live.ps1`, and external VIES behavior still needs deployment smoke.
- `Invoice archive/object storage`: complete production MinIO deployment with Object Lock validation before archive immutability is claimed. The internal/database archive provider remains the development fallback. The code now has a named invoice archive provider router, a direct file-system provider, a reusable `ObjectStorage` abstraction/options boundary, a provider/profile router with centralized container/prefix defaults, an SDK-backed S3-compatible adapter for MinIO/AWS-style storage, Azure Blob archive routing, a generic file-system provider, and optional local MinIO smoke support through Docker Compose and environment-gated tests. Local MinIO smoke passed on 2026-05-12 against a versioned Object Lock bucket with default COMPLIANCE retention. Production direction is MinIO through the S3-compatible provider, with AWS S3 and Azure Blob as alternatives. Validate provider-level retention/versioning/legal-hold behavior with target credentials before claiming production archive immutability.
- `E-invoice`: implement full ZUGFeRD/Factur-X generation and validation, with optional XRechnung export later; structured invoice source-model JSON, minimum source-readiness validation, and a provider-neutral `IEInvoiceGenerationService` boundary now exist. Structured XML downloads, an optional disabled-by-default external command adapter, an adapter smoke harness, and an `IEInvoiceArtifactStorage` persistence boundary also exist. The first tooling path is selected as Mustangproject CLI through the external-command adapter, but no compliant artifact is generated until a pinned wrapper is configured, deterministic fixtures pass, legal validation evidence is added, and deployment smoke succeeds; real ZUGFeRD/Factur-X generation and validation are still required before claiming full e-invoice compliance. XRechnung remains a secondary export backlog item; use `docs/e-invoice-tooling-decision.md` for the selected tooling path and retained alternatives.
- `Mobile external configuration`: provide Google Maps and Firebase/APNS production configuration through secure local/CI/provider configuration. Google Maps rendering was validated locally on the Android emulator on 2026-05-24 after the development key was supplied through local secrets/environment. `Darwin.Mobile.Consumer` still requires the Android Google Maps key through `GoogleMapsApiKey`, `GOOGLE_MAPS_API_KEY`, or `ANDROID_GOOGLE_MAPS_API_KEY`, plus the approved Firebase `google-services.json` at build time for Android Release. iOS/MacCatalyst Release entitlements select production APNS, but signed builds still require Apple Developer provisioning and APNS provider configuration. Real production keys and provider credentials must not be committed; see `docs/external-smoke-inputs.md`.
- `Mobile checkout scope`: decide whether `Darwin.Mobile.Consumer` launches with the current orders/invoices plus hosted retry-payment handoff, or whether full in-app cart/checkout is required before launch. Storefront payment finalization must remain verified-webhook-only either way.

## Completed Internal Go-Live Baseline

- `Business onboarding`: hosted WebAdmin smoke covers creation into inactive `PendingApproval`, approval prerequisite failure, approve/suspend/reactivate lifecycle forms, invitation resend/revoke, accepted/revoked invitation rows staying closed in the operator UI, business-location row-version mutation, and the resumable admin-assisted onboarding wizard with profile, plan, users, locations, loyalty, communications, visibility, and review deep links. WebApi-hosted smoke covers email-confirm enforcement and public visibility against PostgreSQL.
- `Inventory/returns`: hosted operator-flow smoke coverage exercises stock reserve/release, order cancel stock release, return receipt idempotency, refund coordination without unintended stock movement, stock-transfer MarkInTransit/Complete, and purchase-order Issue/Receive through WebAdmin forms. The positive mutation lane now also asserts stock quantities and ledger rows for reserve, release, return receipt, source/destination transfer movement, and supplier receiving.
- `Source contracts`: the current focused Storage/Archive/Invoice/SourceContract lane passed with `154` passed and `0` skipped on 2026-05-12. WebAdmin source-contract (`257` passed, `0` skipped), Business source-contract (`87` passed, `0` skipped), all-source `SecurityAndPerformance` (`615` passed, `0` skipped), and broader Inventory/Business/Invitation/SignIn/Tax/Invoice/VAT (`981` passed, `0` skipped) are clean as of the latest focused runs.
- `Infrastructure tests`: `Darwin.Infrastructure.Tests` now passes locally with `45` passed and `0` skipped after provider-specific design-time factory and injected-clock alignment.
- `Provider smoke readiness`: guarded local smoke harnesses now exist for Stripe test-mode handoff/return-route checks, DHL live validation prerequisites, Brevo sandbox/controlled-inbox readiness, and VIES valid/invalid/provider-failure checks. Source-contract and behavior coverage verifies these scripts keep external calls opt-in, block safely when prerequisites are missing, report ready dry-run status without `-Execute`, and avoid committed secret patterns or raw provider response output.
- `DHL smoke-script contracts`: source-contract coverage now explicitly verifies `scripts/smoke-dhl-live.ps1` keeps runtime pipeline guards (`-RequireRuntimePipeline`) wired to non-secret worker/storage confirmations and keeps the optional return-flow path (`-IncludeReturn`) guarded and explicit.
- `VIES retry`: `RetryUnknownCustomerVatValidationBatchHandler` and the disabled-by-default `VatValidationRetryWorker` retry only provider-generated `Unknown` VAT validation decisions (`provider.unavailable`, `vies.disabled`, `vies.unavailable`) after a configured age. Operator/manual decisions are not overwritten by the retry batch.
- `Go-live readiness dry-run`: `scripts/check-go-live-readiness.ps1` aggregates the secret scan and provider smoke prerequisite checks without executing external provider calls.
- `External smoke inputs`: `docs/external-smoke-inputs.md` lists the required non-committed provider smoke inputs and execution commands for Stripe, DHL, Brevo, and VIES.
- `Consumer mobile hardening`: Android cleartext remains disabled, Consumer Android app backup is disabled, customer-facing promotion diagnostics were removed from the Feed page, legacy template pages were removed, settings navigation moved into the ViewModel, Consumer push registration is now injectable/testable, stale selected commerce details are cleared after refresh failures, and `Darwin.Mobile.Consumer.Tests` passed locally on 2026-05-20 with `119` passed and `0` skipped.
- `Business mobile hardening`: Android cleartext remains disabled, Business Android app backup is disabled, Business subscription remains read-only status plus HTTPS management-website handoff, shared package versions are aligned with Consumer, and `Darwin.Mobile.Business.Tests` passed locally on 2026-05-20 with `11` passed and `0` skipped, guarding Business release hardening, resource parity, soft-gate source contracts, WebAdmin support surfaces, inactive outbox launch scope, safe mobile shell navigation logging, and unsafe certificate trust behavior.
- `Mobile CI guard lanes`: CI now runs `Darwin.Mobile.Shared.Tests`, `Darwin.Mobile.Consumer.Tests`, and `Darwin.Mobile.Business.Tests` on Windows with the MAUI workload installed. CI also runs release-like Windows target builds for all three mobile projects plus Android Debug builds for Consumer and Business. These lanes protect route/security/source contracts, package-version parity, ViewModel behavior that is covered by the current tests, and app-specific launch guards. Android emulator runtime smoke on 2026-05-24 validated Consumer login, Discover, Google Maps, QR generation, Business login, Home, Scan tab, and the non-camera loyalty QR API flow. Mobile is conditionally launch-ready for implemented workflows, while camera-based QR scanning, signed Android/iOS/MacCatalyst release packaging, production mobile configuration, and provider/device smoke remain separate launch work. Tizen is out-of-scope for the current launch target set.

## Near-Term Tasks

- Execute the prioritized test queue in `DarwinTesting.md` section `5.5` before expanding lower-risk backlog items. Current P0 is Stripe business subscription checkout/reconciliation coverage: WebApi-hosted checkout endpoint tests, `CreateSubscriptionCheckoutIntentHandler` tests, subscription `checkout.session.completed` webhook idempotency tests, WebAdmin `Billing/Subscriptions` render/filter tests, and source-contract guards proving return routes remain webhook-only.
- Keep the source-contract lanes green after the 2026-05-10 cleanup completion: WebAdmin source-contract (`257` passed, `0` skipped), Business source-contract (`87` passed, `0` skipped), and all-source `SecurityAndPerformance` (`615` passed, `0` skipped). New source-contract changes should use stable security/localization/HTMX/route/mutation/public-boundary assertions instead of exact copy/layout assertions.
- Complete live-mode Stripe provider validation after operational approval. Test-mode storefront hosted checkout, subscription verified webhook finalization, WebAdmin provider-backed refund reconciliation, and `charge.dispute.created` follow-up processing pass against `https://api.loyan.de`; next repeat the approved smoke with live keys and verify operational monitoring/alerting before production traffic.
- Run DHL live smoke after target account credentials and product settings are entered; start with `scripts/smoke-dhl-live.ps1` prerequisite checks, validate the WebAdmin queued return-label flow, and only use `-Execute` for an approved real validation request.
- Keep the disabled-by-default VIES retry worker off in production until VIES live smoke, retry interval, and support ownership are approved.
- Run `scripts/smoke-vies-live.ps1` with configured valid and invalid VAT IDs before enabling VIES-dependent production review workflows.
- Run `scripts/smoke-brevo-readiness.ps1 -Execute -Sandbox`, then `scripts/smoke-brevo-readiness.ps1 -RequireDeliveryPipeline`, and finally one controlled-inbox `-Execute` pass after Brevo DNS, sender approval, webhook subscription, and worker enablement are complete.
- Run optional local MinIO smoke when Docker Desktop is available, then run selected-provider external smoke for save/read/metadata/presigned URL/object lock or immutability-policy behavior, including profile container/prefix behavior for the invoice archive path, the `ShipmentLabels` profile for DHL labels, and the `MediaAssets` profile for CMS/media uploads. Keep the invoice archive policy wrapper selected only where retention/legal-hold requirements pass.
- Implement the selected Mustangproject CLI wrapper behind `IEInvoiceGenerationService` and wire it through the existing `GenerateInvoiceEInvoiceArtifactHandler`: extend the current structured invoice source-model JSON/XML mapping and readiness validation into the selected ZUGFeRD/Factur-X model, parse validation reports, expose downloadable compliant artifacts only after validation, and add tests. Add XRechnung export later.
- Keep WebApi-hosted business onboarding smoke for email-confirm enforcement and public discovery/detail visibility green against PostgreSQL; keep the WebAdmin hosted lifecycle smoke green as onboarding evolves.
- Keep hosted inventory/returns operator-flow smoke coverage green as the return/RMA workflow evolves.
- Keep the resumable admin-assisted onboarding wizard aligned with existing profile, plan, users, locations, loyalty, communications, visibility, and review workspaces as those underlying handlers evolve.
- Keep hosted inventory/returns smoke assertions tied to stock quantities and ledger rows so future UI changes cannot hide stock double-movement regressions.
- Extend mobile launch-readiness coverage with deeper Consumer and Business ViewModel/UI tests for auth, profile, member-commerce invoice artifacts, business access-state gates, rewards/campaigns, and subscription status handoff beyond the current source-contract guard lanes.
- Extend Consumer app coverage beyond the current launch guards: auth/register/activation, discovery/detail engagement, rewards/QR, member-commerce archive/structured invoice actions, account deletion UI confirmation, and checkout-handoff wording.
- Add mobile release packaging validation for Android Release artifacts and iOS/MacCatalyst signing/entitlements. Keep Google Maps, Firebase/APNS, signing profiles, and store credentials outside the repository.
- Decide whether `Darwin.Mobile.Business` subscription management stays website-only or needs full in-app plans/checkout/cancel-at-period-end flows.
- Decide whether to activate the mobile SQLite outbox beyond inactive scaffolding. Before enabling offline mutations, define the account-switch/business-switch lifecycle, add cleanup semantics, and implement a processor with idempotency and retry visibility.
- Grow WebAdmin CI coverage thresholds only after stable green CI history.

## Later-Phase Tasks

- Carrier-integrated DHL RMA/returns automation beyond the current return-label queue path, returns queue, and shipment provider operations.
- Brevo provider-side template authoring/synchronization workflow, if operations decide to manage template bodies in Brevo instead of Darwin settings. Darwin can already map configured Brevo template IDs to Darwin template keys for send-time delivery.
- External immutable invoice archive provider hardening: MinIO/S3-compatible or Azure Blob external smoke, bucket/container Object Lock or immutability-policy validation, backup, restore, and least-privilege access review.
- Full ZUGFeRD/Factur-X plus secondary XRechnung export implementation.
- Investigate adding asynchronous retry for VIES VAT validation beyond the current disabled-by-default retry worker policy, including operational ownership, retry cadence, and provider failure alerting.
- Consider self-service onboarding in `Darwin.Web` after WebAdmin/admin-assisted onboarding is stable.
- Keep Android build warnings and WebAdmin analyzer warnings at zero after the 2026-05-13 SDK/workload repair, Android API guard cleanup, and `xUnit1051` test cleanup.
- Additional public catalog/CMS facet/search projection improvements for scale.

## Open Decisions

- Object storage deployment profile: MinIO is the recommended self-hosted target through the S3-compatible provider; deployments may still choose AWS S3 or Azure Blob. Exact endpoint, bucket/container, Object Lock/retention/legal-hold settings, backup, and restore policy must be confirmed per deployment.
- E-invoice implementation packaging for the selected Mustangproject CLI path: pin artifact, approve license/security review, add wrapper, parse validation reports, and run smoke. Retained alternatives are documented in `docs/e-invoice-tooling-decision.md`.
- Exact DHL account/product-code contract after target-account live smoke.
- When to rotate from Stripe test keys to live keys, and which restricted live key permissions are sufficient for Checkout Session creation and webhook reconciliation.

## Active Handoff Summary

- PostgreSQL remains the preferred/default runtime provider; SQL Server remains supported.
- WebAdmin remains MVC/Razor + HTMX.
- `Darwin.Web` remains a separate Next.js customer-facing application and must not receive operator diagnostics.
- Storefront payment completion remains verified-webhook-only.
- No provider secrets belong in git, docs, logs, or test output.
- Compliance planning decisions are summarized in `docs/compliance-decisions.md`.
